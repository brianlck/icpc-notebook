#include "general.h"

int main() {
  cin.tie(0)->sync_with_stdio(false);
  int t = 1;
  // cin >> t;
  for (int i = 1; i <= t; ++i) {
    // cout << "Case #" << i << ": ";
    solve(i);
  }
}

void solve(int case_id) {}